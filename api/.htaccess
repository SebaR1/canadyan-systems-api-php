<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /canadian-sistemas/api/

    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule ^ - [L]

    RewriteRule ^(.*)$ $1 [L,QSA]
</IfModule>

<FilesMatch "^\.env">
    Order allow,deny
    Deny from all
</FilesMatch>

Options -Indexes
